meta {
  name: Create Project
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/projects
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Campanha Black Friday 1"
  }
}

script:post-response {
  const data = res.getBody();
  
  if (data.id) {
    bru.setEnvVar("projectId", data.id);
  }
}
